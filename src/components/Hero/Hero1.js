import React from 'react'

const Hero1 = ({slides}) => {
    return (
        <div>
            {slides.map(slides, index) => {(
                
            )}}
        </div>
    )
}

export default Hero1
